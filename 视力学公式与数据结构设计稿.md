# 视力学公式与数据结构设计稿

## 1. 文档目标

- 本文档用于定义视力自测应用的核心视力学公式、显示物理公式、标准化测试流程和数据结构
- 本文档服务于后续 Flutter/Dart 实现
- 本文档以小数视力为主结果标准，以五分制对数视力为辅助显示，以 logMAR / MAR 为内部计算核心
- 本文档只覆盖远视力 E 视标测试，不覆盖散光、色觉、立体视和近视防控策略

## 2. 统一约定

### 2.1 单位约定

- 长度默认单位：mm
- 距离默认单位：mm
- 角度默认单位：arcmin 或 rad
- 分辨率单位：px
- 时间单位：ms

### 2.2 结果约定

- 主结果：小数视力 decimal acuity
- 辅助结果：五分制对数视力
- 内部计算主量：logMAR

### 2.3 标准 E 视标几何约定

- E 视标采用 5 × 5 单元结构
- 总宽度 = 5 个单元
- 总高度 = 5 个单元
- 笔画宽度 = 1 个单元
- 缺口宽度 = 1 个单元
- 关键细节尺寸 = 1 个单元
- 整体视角 = 5 × 关键细节视角

### 2.4 标准来源约定

- 上位标准文件：`临床视力测试标准基线.md`
- 项目测量标准：ETDRS 标准化流程
- 临床兼容标准：常见 5 米 E 视力表/标准对数视力表行读流程
- 废止默认口径：
  - `3-down 1-up`
  - `6 次反转终止`
  - `反转点均值估计阈值`
- 若旧代码仍保留 `staircase` 字段，只能视为迁移期兼容字段

## 3. 核心视力学公式

### 3.1 MAR 与 logMAR

- `MAR = 10^(logMAR)`
- `logMAR = log10(MAR)`

说明：

- MAR 是最小分辨角，单位为 arcmin
- 当 `MAR = 1` 时，对应正常基准
- 当 `logMAR = 0` 时，对应小数视力 1.0

### 3.2 小数视力与 logMAR

- `decimalAcuity = 1 / MAR`
- `decimalAcuity = 10^(-logMAR)`
- `logMAR = -log10(decimalAcuity)`

常用示例：

- 小数视力 1.0 → logMAR = 0.0
- 小数视力 0.8 → logMAR ≈ 0.097
- 小数视力 0.5 → logMAR ≈ 0.301
- 小数视力 1.5 → logMAR ≈ -0.176

### 3.3 五分制对数视力与小数视力、logMAR

- `fivePointAcuity = 5.0 + log10(decimalAcuity)`
- `fivePointAcuity = 5.0 - logMAR`
- `decimalAcuity = 10^(fivePointAcuity - 5.0)`
- `logMAR = 5.0 - fivePointAcuity`

常用示例：

- 小数视力 0.1 → 五分制对数视力 4.0
- 小数视力 0.5 → 五分制对数视力约 4.7
- 小数视力 1.0 → 五分制对数视力 5.0
- 小数视力 1.5 → 五分制对数视力约 5.18

## 4. 视标尺寸公式

### 4.1 关键细节角与整体视角

- 关键细节角 = `MAR` arcmin
- E 视标整体视角 = `5 × MAR` arcmin

记号：

- `detailAngleArcmin = MAR`
- `optotypeAngleArcmin = 5 × MAR`

### 4.2 arcmin 转 rad

- `arcminToDegree = arcmin / 60`
- `degreeToRad = degree × π / 180`
- `arcminToRad = arcmin × π / (180 × 60)`

因此：

- `optotypeAngleRad = optotypeAngleArcmin × π / 10800`
- `detailAngleRad = detailAngleArcmin × π / 10800`

### 4.3 视标物理尺寸

给定测试距离 `testDistanceMm`，整体视标物理尺寸：

- `optotypeSizeMm = 2 × testDistanceMm × tan(optotypeAngleRad / 2)`

关键细节物理尺寸：

- `detailSizeMm = optotypeSizeMm / 5`

也可直接写为：

- `detailSizeMm = 2 × testDistanceMm × tan(detailAngleRad / 2)`

在小角度条件下，工程上可近似：

- `size ≈ distance × angleRad`

但最终实现仍应使用正切公式，避免在极端条件下产生累积误差。

### 4.4 视标像素尺寸

给定屏幕参数：

- `screenWidthMm`
- `screenHeightMm`
- `screenWidthPx`
- `screenHeightPx`

则：

- `pixelWidthMm = screenWidthMm / screenWidthPx`
- `pixelHeightMm = screenHeightMm / screenHeightPx`

视标像素尺寸：

- `optotypeWidthPx = optotypeSizeMm / pixelWidthMm`
- `optotypeHeightPx = optotypeSizeMm / pixelHeightMm`
- `detailWidthPx = detailSizeMm / pixelWidthMm`
- `detailHeightPx = detailSizeMm / pixelHeightMm`

渲染时建议：

- 取 `min(detailWidthPx, detailHeightPx)` 作为关键细节安全判定依据
- 取整策略统一为：
  - 计算阶段保留浮点
  - 绘制阶段统一 round
  - 阈值判断阶段统一使用浮点原值

## 5. 像素瓶颈约束

### 5.1 原则

- 若关键细节尺寸太小，屏幕无法正确表达目标视标
- 一旦进入该区域，测试不应继续用于判定更高视力档位

### 5.2 判定公式

- `criticalDetailPx = min(detailWidthPx, detailHeightPx)`

建议首版阈值：

- `criticalDetailPx >= 3.0`：允许继续
- `2.0 <= criticalDetailPx < 3.0`：进入警告区，可提示用户结果边界风险
- `criticalDetailPx < 2.0`：禁止继续向更高视力档位推进

### 5.3 处理策略

- 若像素瓶颈触发：
  - 记录当前设备达到的理论上限
  - 停止向更小视标递进
  - 提示用户拉大测试距离或更换更高分辨率设备

## 6. 测试档位设计

### 6.1 内部档位组织

- 内部档位按 logMAR 组织
- 首版建议步长：0.1 logMAR
- 档位范围建议：
  - 最低：1.0
  - 最高：-0.3

示例档位：

| logMAR | 小数视力 | 五分制对数视力 |
| --- | --- | --- |
| 1.0 | 0.1 | 4.0 |
| 0.9 | 0.126 | 4.1 |
| 0.8 | 0.158 | 4.2 |
| 0.7 | 0.200 | 4.3 |
| 0.6 | 0.251 | 4.4 |
| 0.5 | 0.316 | 4.5 |
| 0.4 | 0.398 | 4.6 |
| 0.3 | 0.501 | 4.7 |
| 0.2 | 0.631 | 4.8 |
| 0.1 | 0.794 | 4.9 |
| 0.0 | 1.000 | 5.0 |
| -0.1 | 1.259 | 5.1 |
| -0.2 | 1.585 | 5.2 |
| -0.3 | 1.995 | 5.3 |

### 6.2 外部结果显示规则

- 对用户主显示：
  - 小数视力保留 1 到 2 位有效小数
- 对用户辅助显示：
  - 五分制对数视力保留 1 位小数
- 对内部分析：
  - logMAR 保留至少 3 位小数

## 7. 测试流程与结果判定逻辑

### 7.1 任务类型

- E 视标方向识别属于四选一强迫选择任务
- 猜测概率 = 25%

### 7.2 临床常见行读流程

- 标准距离通常为 `5 m`
- 从大到小逐行读取
- 记录“通过的最小一行”
- 行通过规则采用：
  - `correctCountInLine > optotypeCountInLine / 2`

### 7.3 ETDRS 标准化流程

- 标准距离：`4 m`
- 每行固定 `5` 个视标
- 每个视标只允许作答一次
- 每行之间固定 `0.1 logMAR` 步长
- 每个视标对应 `0.02 logMAR`
- 逐行读取，按逐视标正确数计分

### 7.4 终止与补测规则

- 当前行错误数超过协议允许阈值时停止继续向更小行推进
- 若 `4 m` 下正确识别数 `< 20`，应进入 `1 m` 近距补测流程
- 达到最大工程保护题量 `50` 时停止
- 进入像素瓶颈极限时停止
- 用户主动退出时停止

### 7.5 结果计算

- 临床兼容结果：
  - `bestLineLogMar`
  - `bestDecimalAcuity`
  - `bestFivePointAcuity`
- ETDRS 标准化结果：
  - `etdrsLetterScore`
  - `equivalentLogMar`
- ETDRS 常用换算：
  - `letterScore = correctAt4m + 30`（当 `correctAt4m >= 20`）
  - `equivalentLogMar = 1.7 - 0.02 * letterScore`

### 7.6 可信度指标

可定义以下指标：

- `lineConsistencyScore`
- `responseAccuracy`
- `meanResponseTimeMs`
- `pixelMarginRatio`

首版可给出简单规则：

- 行级表现稳定：可信度高
- 行级表现波动明显：提示建议重测
- 多次超时或反应异常慢：提示可能存在疲劳或环境干扰

## 8. 拥挤效应模式

### 8.1 模式定义

- `isolated`：仅显示单个 E 视标
- `crowded`：显示目标 E 视标和 flankers

### 8.2 设计目标

- `isolated` 用于基础快速测试
- `crowded` 用于更接近临床整行识别条件

### 8.3 数据要求

- 每条题目记录需要保存当前显示模式
- 结果页需要明确标识本次测试属于哪种模式

## 9. 屏幕与测试配置数据结构

以下结构以 Dart 风格定义。

```dart
enum EyeSide {
  left,
  right,
  both,
}

enum OptotypeDirection {
  up,
  down,
  left,
  right,
}

enum TestMode {
  isolated,
  crowded,
}

enum InputMode {
  keyboard,
  touchButtons,
  swipe,
}

enum SessionEndReason {
  protocolCompleted,
  maxQuestionsReached,
  pixelLimitReached,
  userAborted,
}
```

```dart
class ScreenProfile {
  final String id;
  final String deviceName;
  final double screenWidthMm;
  final double screenHeightMm;
  final int screenWidthPx;
  final int screenHeightPx;
  final double pixelWidthMm;
  final double pixelHeightMm;
  final double devicePixelRatio;
  final bool isDpiAware;
  final DateTime createdAt;
  final DateTime updatedAt;

  const ScreenProfile({
    required this.id,
    required this.deviceName,
    required this.screenWidthMm,
    required this.screenHeightMm,
    required this.screenWidthPx,
    required this.screenHeightPx,
    required this.pixelWidthMm,
    required this.pixelHeightMm,
    required this.devicePixelRatio,
    required this.isDpiAware,
    required this.createdAt,
    required this.updatedAt,
  });
}
```

```dart
class CalibrationProfile {
  final String id;
  final String screenProfileId;
  final bool userEnteredPhysicalSize;
  final bool cardCalibrationUsed;
  final double? referenceObjectWidthMm;
  final double? referenceObjectHeightMm;
  final double calibrationScaleX;
  final double calibrationScaleY;
  final double confidenceScore;
  final DateTime createdAt;

  const CalibrationProfile({
    required this.id,
    required this.screenProfileId,
    required this.userEnteredPhysicalSize,
    required this.cardCalibrationUsed,
    required this.referenceObjectWidthMm,
    required this.referenceObjectHeightMm,
    required this.calibrationScaleX,
    required this.calibrationScaleY,
    required this.confidenceScore,
    required this.createdAt,
  });
}
```

```dart
class TestConfig {
  final String id;
  final EyeSide eyeSide;
  final TestMode testMode;
  final InputMode inputMode;
  final String protocol;
  final double testDistanceMm;
  final double? nearFallbackDistanceMm;
  final double startLogMar;
  final double minLogMar;
  final double maxLogMar;
  final double stepLogMar;
  final int optotypesPerLine;
  final int maxErrorsPerLine;
  final int minimumCorrectPerLine;
  final int maxQuestionCount;
  final int answerTimeLimitMs;
  final double minCriticalDetailPx;
  final bool enableEnvironmentCheck;
  final bool enablePixelLimitProtection;

  const TestConfig({
    required this.id,
    required this.eyeSide,
    required this.testMode,
    required this.inputMode,
    required this.protocol,
    required this.testDistanceMm,
    required this.nearFallbackDistanceMm,
    required this.startLogMar,
    required this.minLogMar,
    required this.maxLogMar,
    required this.stepLogMar,
    required this.optotypesPerLine,
    required this.maxErrorsPerLine,
    required this.minimumCorrectPerLine,
    required this.maxQuestionCount,
    required this.answerTimeLimitMs,
    required this.minCriticalDetailPx,
    required this.enableEnvironmentCheck,
    required this.enablePixelLimitProtection,
  });
}
```

## 10. 视力学中间计算结构

```dart
class AcuityLevel {
  final double logMar;
  final double marArcmin;
  final double decimalAcuity;
  final double fivePointAcuity;
  final double optotypeAngleArcmin;
  final double detailAngleArcmin;

  const AcuityLevel({
    required this.logMar,
    required this.marArcmin,
    required this.decimalAcuity,
    required this.fivePointAcuity,
    required this.optotypeAngleArcmin,
    required this.detailAngleArcmin,
  });
}
```

```dart
class RenderMetrics {
  final double testDistanceMm;
  final double optotypeSizeMm;
  final double detailSizeMm;
  final double optotypeWidthPx;
  final double optotypeHeightPx;
  final double detailWidthPx;
  final double detailHeightPx;
  final double criticalDetailPx;
  final bool pixelLimitReached;

  const RenderMetrics({
    required this.testDistanceMm,
    required this.optotypeSizeMm,
    required this.detailSizeMm,
    required this.optotypeWidthPx,
    required this.optotypeHeightPx,
    required this.detailWidthPx,
    required this.detailHeightPx,
    required this.criticalDetailPx,
    required this.pixelLimitReached,
  });
}
```

## 11. 题目、进度与会话数据结构

```dart
class QuestionRecord {
  final int index;
  final EyeSide eyeSide;
  final TestMode testMode;
  final int lineIndex;
  final int optotypeIndexInLine;
  final double targetLogMar;
  final double targetDecimalAcuity;
  final double targetFivePointAcuity;
  final OptotypeDirection displayedDirection;
  final OptotypeDirection? userAnswer;
  final bool isCorrect;
  final bool isTimeout;
  final int responseTimeMs;
  final RenderMetrics renderMetrics;
  final DateTime shownAt;
  final DateTime answeredAt;

  const QuestionRecord({
    required this.index,
    required this.eyeSide,
    required this.testMode,
    required this.lineIndex,
    required this.optotypeIndexInLine,
    required this.targetLogMar,
    required this.targetDecimalAcuity,
    required this.targetFivePointAcuity,
    required this.displayedDirection,
    required this.userAnswer,
    required this.isCorrect,
    required this.isTimeout,
    required this.responseTimeMs,
    required this.renderMetrics,
    required this.shownAt,
    required this.answeredAt,
  });
}
```

```dart
class LineProgressState {
  final int currentLineIndex;
  final double currentLogMar;
  final int currentLinePresentedCount;
  final int currentLineCorrectCount;
  final int totalPresentedCount;
  final int totalCorrectCount;
  final bool usedNearFallback;
  final bool protocolCompleted;

  const LineProgressState({
    required this.currentLineIndex,
    required this.currentLogMar,
    required this.currentLinePresentedCount,
    required this.currentLineCorrectCount,
    required this.totalPresentedCount,
    required this.totalCorrectCount,
    required this.usedNearFallback,
    required this.protocolCompleted,
  });
}
```

```dart
class EyeTestResult {
  final EyeSide eyeSide;
  final double equivalentLogMar;
  final double bestLineLogMar;
  final double decimalAcuity;
  final double fivePointAcuity;
  final int etdrsLetterScore;
  final int totalQuestions;
  final int correctQuestions;
  final double accuracy;
  final double meanResponseTimeMs;
  final double lineConsistencyScore;
  final bool pixelLimitEncountered;
  final bool usedNearFallback;
  final bool retestRecommended;

  const EyeTestResult({
    required this.eyeSide,
    required this.equivalentLogMar,
    required this.bestLineLogMar,
    required this.decimalAcuity,
    required this.fivePointAcuity,
    required this.etdrsLetterScore,
    required this.totalQuestions,
    required this.correctQuestions,
    required this.accuracy,
    required this.meanResponseTimeMs,
    required this.lineConsistencyScore,
    required this.pixelLimitEncountered,
    required this.usedNearFallback,
    required this.retestRecommended,
  });
}
```

```dart
class TestSession {
  final String id;
  final String screenProfileId;
  final String calibrationProfileId;
  final TestConfig config;
  final SessionEndReason endReason;
  final List<QuestionRecord> questions;
  final List<EyeTestResult> eyeResults;
  final DateTime startedAt;
  final DateTime finishedAt;

  const TestSession({
    required this.id,
    required this.screenProfileId,
    required this.calibrationProfileId,
    required this.config,
    required this.endReason,
    required this.questions,
    required this.eyeResults,
    required this.startedAt,
    required this.finishedAt,
  });
}
```

## 12. 计算流程设计

### 12.1 单题生成流程

1. 读取当前 `LineProgressState.currentLogMar`
2. 计算 `AcuityLevel`
3. 读取 `ScreenProfile` 和 `TestConfig.testDistanceMm`
4. 计算 `RenderMetrics`
5. 检查 `criticalDetailPx`
6. 若未触发像素极限，则按当前行位置生成方向并渲染题目
7. 记录题目开始时间

### 12.2 单题判定流程

1. 接收用户方向输入
2. 判定正误与是否超时
3. 生成 `QuestionRecord`
4. 更新当前行已展示数与当前行正确数
5. 判断当前行是否达到停止或推进条件
6. 更新 `LineProgressState`
7. 判断是否满足测试终止条件

### 12.3 单眼结果生成流程

1. 汇总当前眼全部 `QuestionRecord`
2. 计算 `bestLineLogMar` 与 `etdrsLetterScore`
3. 换算得到小数视力和五分制对数视力
4. 计算正确率、平均反应时、行级稳定性
5. 输出 `EyeTestResult`

## 13. 建议的工具函数接口

```dart
double marFromLogMar(double logMar);
double logMarFromMar(double marArcmin);
double decimalFromLogMar(double logMar);
double logMarFromDecimal(double decimalAcuity);
double fivePointFromLogMar(double logMar);
double logMarFromFivePoint(double fivePointAcuity);
double arcminToRad(double arcmin);
double optotypeSizeMm(double logMar, double testDistanceMm);
double detailSizeMm(double logMar, double testDistanceMm);
RenderMetrics calculateRenderMetrics({
  required double logMar,
  required double testDistanceMm,
  required ScreenProfile screenProfile,
});
bool isPixelLimitReached(RenderMetrics metrics, double minCriticalDetailPx);
int calculateEtDrsLetterScore({
  required int correctAtFourMeters,
  int correctAtOneMeter = 0,
  required bool usedNearFallback,
});
double equivalentLogMarFromEtDrsLetterScore(int letterScore);
double calculateClinicalLineLogMar({
  required int lineIndex,
  required List<QuestionRecord> questions,
  required int minimumCorrectPerLine,
});
```

## 14. 首版默认参数建议

| 参数 | 建议值 |
| --- | --- |
| 起始 logMAR | 0.5 |
| 最小 logMAR | -0.3 |
| 最大 logMAR | 1.0 |
| 步长 | 0.1 |
| 协议 | ETDRS |
| 标准测试距离 | 4000 mm |
| 近距补测距离 | 1000 mm |
| 每行视标数 | 5 |
| 单行最大允许错误数 | 2 |
| 临床兼容行通过阈值 | 正确数大于该行一半 |
| 最大题量保护 | 50 |
| 单题作答时限 | 3000 ms |
| 像素安全阈值 | 3.0 px |

## 15. 首版必须验证的计算样例

至少建立以下测试样例：

- 小数视力 1.0 ↔ logMAR 0.0 ↔ 五分制对数视力 5.0
- 小数视力 0.5 ↔ logMAR 0.3010 ↔ 五分制对数视力约 4.7
- 小数视力 0.1 ↔ logMAR 1.0 ↔ 五分制对数视力 4.0
- ETDRS `letterScore = 85` ↔ `logMAR = 0.0`
- 给定 5 米测试距离时，logMAR 0.0 的 E 视标整体物理尺寸计算
- 给定屏幕宽高和分辨率时，关键细节像素尺寸计算
- 当关键细节像素小于 3.0 时，像素瓶颈保护是否正确触发

## 16. 实现结论

- 产品显示层应以小数视力为主，五分制对数视力为辅
- 产品测量层应以 logMAR 组织所有档位、阈值和结果估计
- 视标尺寸必须由测试距离和视角公式反推，不能使用经验字号
- 像素瓶颈保护是保证工程可信度的必要约束
- 项目默认流程应采用 ETDRS 标准化流程，并保留临床常见行读结果映射
- 数据结构应围绕屏幕配置、视力学计算、题目记录、行级推进和测试会话来组织

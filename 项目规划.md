# 视力表自测应用项目规划

## 1. 产品定位

- 产品目标：开发一个在屏幕上显示标准 E 视标、支持用户自助完成远视力测试的应用
- 设计原则：整个产品逻辑严格以视敏度测量的医学原理和显示尺寸计算的物理原理为基础，不以市面上既有传统视力表的实现方式作为主导依据
- 核心思想：先定义视觉阈值、视角、视标几何和记录体系，再由这些基础推导交互流程、尺寸计算、测试算法和结果展示
- 标准来源层级：
  - 上位标准：`临床视力测试标准基线.md`
  - 测量引擎标准：ETDRS 标准化流程
  - 临床兼容参考：常见 5 米 E 视力表/标准对数视力表行读流程
- 定位方向：提供专业化的自测与筛查体验，但结果性质仍为参考，不替代临床正式诊断
- 结果体系：以小数视力标准作为主结果表达，辅助显示五分制对数视力（缪天荣标准），不考虑分数视力
- 支持平台：桌面端与手机端
- 交互方式：
  - 桌面端主要使用键盘方向键作答
  - 手机端主要使用屏幕方向按钮作答

## 2. 医学与物理基础

### 2.1 视敏度基础

- 本应用的核心测量对象是视敏度（Visual Acuity）
- 视敏度本质上反映人眼分辨空间细节的能力
- 在临床和视力表设计中，通常以最小分辨角 MAR 作为基础描述量
- 正常基准可定义为关键细节对应 1 分视角，即 1 arcmin

### 2.2 最小分辨角与标准视标关系

- MAR 表示最小分辨角，单位为 arcmin
- 当 MAR = 1 arcmin 时，对应小数视力 1.0
- 标准视标的关键细节应对应 1 个 MAR
- 对 E 视标而言：
  - 单笔画宽度 = 1 个关键细节角
  - 缺口宽度 = 1 个关键细节角
  - 总高度 = 5 个关键细节角
  - 总宽度 = 5 个关键细节角
- 因此当目标是正常视力阈值时：
  - E 的笔画宽度对应 1 arcmin
  - E 的整体外框对应 5 arcmin

### 2.3 小数视力、五分制对数视力与 logMAR 的统一关系

- 产品主结果表达应以小数视力为核心
- 内部计算体系仍应以 MAR / logMAR 为核心
- 定义关系：
  - `MAR = 10^(logMAR)`
  - `decimalAcuity = 1 / MAR = 10^(-logMAR)`
- 五分制对数视力可按缪天荣标准映射：
  - `fivePointAcuity = 5.0 + log10(decimalAcuity)`
  - 等价写法：`fivePointAcuity = 5.0 - logMAR`
- 产品结果应支持以下表达层次：
  - 小数视力：主显示与主记录标准
  - 五分制对数视力：辅助显示
  - logMAR：内部算法与专业分析字段

### 2.4 视标角尺寸计算原则

- 对任意目标 logMAR，先计算：
  - `MAR = 10^(logMAR)` arcmin
  - `optotypeAngle = 5 × MAR` arcmin
- 视标总高度和总宽度的物理尺寸应由测试距离和视角推导：
  - `optotypeSizeMm = 2 × testDistanceMm × tan(optotypeAngleRad / 2)`
- 关键细节尺寸应为：
  - `strokeOrGapMm = optotypeSizeMm / 5`
- 在小角度条件下可以采用近似换算，但工程实现中应保留基于正切的标准公式

### 2.5 显示设备物理映射原则

- 用户必须输入屏幕的物理宽度和物理高度两个数据
- 不采用仅输入对角线尺寸作为核心计算依据
- 软件读取屏幕分辨率：
  - 水平分辨率 `screenWidthPx`
  - 垂直分辨率 `screenHeightPx`
- 用户输入屏幕物理尺寸：
  - 屏幕物理宽度 `screenWidthMm`
  - 屏幕物理高度 `screenHeightMm`
- 计算规则：
  - 每像素物理宽度 = `screenWidthMm / screenWidthPx`
  - 每像素物理高度 = `screenHeightMm / screenHeightPx`
- 视标最终绘制时，应按目标物理尺寸分别映射到像素宽高
- 若水平方向与垂直方向的物理像素尺寸存在差异，应分别计算，不能直接使用单一 DPI 替代

### 2.6 四选一识别任务与猜测概率

- E 视标方向识别本质上是四选一强迫选择任务
- 随机猜测的机会水平为 25%
- 因此测试引擎不能把“答对率”简单等同于真实视力阈值
- 阈值判定应考虑：
  - 机会水平校正
  - 标准化测试协议对应的计分法
  - 最小题量
  - 重复测量稳定性

### 2.7 显示像素瓶颈原则

- 显示设备存在最小可渲染尺寸限制
- 当目标视标的关键细节宽度过小，可能低于屏幕的有效像素分辨能力
- 因此系统在每次渲染前都应检查：
  - 目标笔画宽度对应的像素数
  - 目标缺口宽度对应的像素数
- 若关键细节小于安全阈值，则本次测试结果不应继续向更高视力推进
- 首版建议设置最小安全渲染阈值，例如关键细节至少占 3 个像素，再视设备验证结果调整

### 2.8 拥挤效应原则

- 单个孤立视标的识别结果可能高于临床整行视标条件下的结果
- 为了更接近临床视觉任务，应考虑拥挤效应对识别阈值的影响
- 因此测试模式应预留两种显示条件：
  - 单视标模式
  - 拥挤效应模式
- 拥挤效应模式可通过 E 视标周围的 flankers 或边框元素来模拟

## 3. 关键前提与设计原则

### 3.1 测试距离原则

- 测试距离由软件指定
- 测试距离由用户自行遵守
- 软件不承担自动测距职责
- 软件必须在测试前明确提示：
  - 测试距离
  - 保持站位稳定
  - 不可在测试中途前后移动
- 所有视标尺寸推导和结果换算都必须绑定当前测试距离
- 高级版本可扩展手机端实时测距能力，用于距离提醒或动态修正，但不作为首版成立前提

### 3.2 Windows 缩放处理原则

- 测试逻辑必须尽可能基于真实像素尺寸而非逻辑像素
- 软件不能直接依赖受系统缩放影响后的界面单位
- 目标不是简单“忽略缩放”，而是：
  - 获取真实显示像素信息
  - 在渲染链路中修正缩放比例
  - 保证输入的物理尺寸能准确映射到最终显示像素

### 3.3 视标绘制原则

- 使用标准 E 视标
- 不建议使用位图缩放
- 建议以代码绘制矢量化 E 视标
- 必须保证：
  - 总边长
  - 笔画宽度
  - 缺口宽度
  - 方向旋转
  都由统一参数控制
- 所有尺寸参数必须由目标 logMAR 和测试距离反推，而不是手工配置固定字号

### 3.4 内部数据表达原则

- 对用户展示时，小数视力应为主表达
- 五分制对数视力作为辅助显示层存在
- 视力阈值的内部计算主表达仍应为 logMAR
- 测试记录应同时保存：
  - 测试距离
  - 目标 logMAR
  - 对应 MAR
  - 对应小数视力
  - 对应五分制对数视力
  - 设备物理参数

## 4. 产品需求清单

### 4.1 用户需求

- 用户可以在家中完成单眼或双眼的远视力测试
- 用户可以通过键盘或触屏快速完成作答
- 用户可以获得严格基于视角与视敏度模型计算出的结果
- 用户可以以小数视力作为主结果查看测试结果
- 用户可以辅助查看五分制对数视力结果
- 用户可以重复测试并查看历史变化
- 用户可以在测试前配置设备参数与测试距离

### 4.2 功能需求

#### 4.2.1 设备配置

- 支持用户输入屏幕物理宽度
- 支持用户输入屏幕物理高度
- 支持软件读取屏幕分辨率
- 支持保存设备配置参数
- 支持重新编辑设备参数
- 支持显示当前像素物理宽度与像素物理高度

#### 4.2.2 测试配置

- 支持设置测试距离
- 支持选择测试眼别：
  - 左眼
  - 右眼
  - 双眼
- 支持设置输出记录体系：
  - 小数视力主显示
  - 五分制对数视力辅助显示
- 支持测试前提示：
  - 遮眼方式
  - 测试距离
  - 保持姿势
  - 环境光建议
  - 结果参考性质

#### 4.2.3 测试执行

- 显示标准 E 视标
- 视标方向随机
- 出题顺序随机
- 支持桌面端键盘方向键输入
- 支持手机端触屏方向按钮输入
- 支持后续扩展手机端滑动手势输入
- 根据标准化测试协议自动推进到下一行或结束测试
- 支持暂停测试
- 支持重测
- 支持中止测试
- 支持单题作答时限策略
- 支持拥挤效应显示模式
- 支持记录每题：
  - 目标 logMAR
  - 视标像素尺寸
  - 正误
  - 反应时间

#### 4.2.4 结果与记录

- 输出单次测试结果
- 输出左右眼结果
- 输出本次测试距离
- 输出使用的屏幕配置参数
- 输出题目数与正确率
- 输出最终标准化结果对应的：
  - 小数视力
  - 五分制对数视力
  - logMAR
  - ETDRS letter score
- 保存历史记录
- 支持查看历史趋势

### 4.3 非功能需求

- 支持全屏测试
- 输入响应低延迟
- 离线可运行
- 本地保存数据
- 不依赖联网
- 不采集不必要隐私信息
- 计算逻辑应可单元测试验证
- 关键尺寸渲染应具备像素下限保护

### 4.4 专业性约束

- 产品的尺寸计算、阈值逻辑、结果换算必须以医学与物理公式为准
- 不允许再把 `3-down 1-up`、`反转次数`、`反转点均值` 写成项目默认标准
- 内部主数据结构必须能表达“行级推进 + 逐视标记录 + ETDRS 分数”
- 临床常见行别展示可以保留，但必须明确它是兼容输出，不是唯一测量依据
- 结果展示应以小数视力为主，五分制对数视力为辅
- 产品目标为专业化体验，不等同于医疗诊断
- 需要在结果页显示参考性质说明
- 需要提醒用户：
  - 如结果异常
  - 如双眼差异较大
  - 如多次测试波动明显
  应尽快到线下做正规检查

## 5. MVP 功能列表

### 5.1 MVP 目标

- 跑通从设备配置到正式测试再到结果输出的完整闭环
- 以小数视力为主结果、logMAR 为内部计算核心完成一版可验证的 ETDRS 标准化测量流程
- 首先验证尺寸换算、标准化结果判定、跨平台交互是否稳定
- 优先保证医学与物理一致性，而不是扩展过多功能

### 5.2 MVP 必做功能

- 输入屏幕物理宽度
- 输入屏幕物理高度
- 读取当前屏幕分辨率
- 根据物理尺寸和分辨率计算像素物理尺寸
- 输入测试距离
- 按 logMAR 档位计算目标视标物理尺寸
- E 视标自绘
- 四方向随机出题
- 基于 ETDRS 行级推进规则的标准化测试流程
- 渲染前的像素瓶颈检查
- 单眼测试流程
- 左眼与右眼分别测试
- 桌面端方向键输入
- 手机端方向按钮输入
- 测试前说明页
- 结果页主显示小数视力并辅助显示五分制对数视力
- 本地保存历史记录

### 5.3 MVP 建议纳入功能

- 全屏模式
- Windows DPI 感知与缩放修正
- 横屏测试页
- 答题反应时间记录
- 结果可信度提示
- 历史记录列表
- 按 0.1 logMAR 步长组织测试档位
- 银行卡/身份证贴屏校准
- 拥挤效应显示模式
- 手机端滑动手势输入
- 环境光条件检测

### 5.4 MVP 暂不纳入功能

- 摄像头自动测距
- 医疗机构级报告
- 多视标种类
- 色觉测试
- 散光测试
- 云同步
- 多用户账号体系
- 儿童模式
- 语音播报

## 6. 技术选型方案

### 6.1 总体选型

- 推荐框架：Flutter

### 6.2 选择 Flutter 的原因

- 一套代码覆盖桌面端和手机端
- 适合做高精度自定义绘制
- 适合统一管理输入事件
- 支持 Windows、Android、iOS
- 页面流转、状态管理和本地存储都较成熟
- 比纯 Web 更容易控制全屏和像素级渲染

### 6.3 不优先选择纯 Web 的原因

- 浏览器中的像素、缩放、设备像素比更复杂
- 物理尺寸映射更容易受浏览器环境影响
- 键盘焦点、全屏和移动端一致性较难保证

### 6.4 架构模块划分

#### 6.4.1 设备参数模块

- 采集屏幕物理宽度
- 采集屏幕物理高度
- 获取当前分辨率
- 保存当前设备参数
- 提供像素物理尺寸计算结果

#### 6.4.2 视力学模型模块

- 根据 logMAR 计算 MAR
- 根据 MAR 计算 E 视标整体视角
- 根据测试距离计算目标物理尺寸
- 根据设备物理参数计算目标像素尺寸
- 提供 logMAR、小数视力、五分制对数视力的换算
- 提供像素瓶颈校验能力

#### 6.4.3 视标绘制模块

- 根据目标物理尺寸绘制 E 视标
- 保证外框为 5 单位结构
- 保证笔画和缺口为 1 单位结构
- 支持四方向旋转
- 支持不同档位尺寸切换
- 不依赖位图缩放

#### 6.4.4 测试引擎模块

- 生成随机方向题目
- 管理当前 logMAR 档位
- 根据作答结果推进当前行、统计逐视标结果并生成标准化分数
- 记录答题历史
- 决定终止条件
- 输出最终结果

#### 6.4.5 测试流程状态模块

- 未配置
- 已配置
- 准备测试
- 测试中
- 暂停
- 已完成

#### 6.4.6 本地存储模块

- 保存设备参数
- 保存测试配置
- 保存测试结果
- 保存历史记录

### 6.5 推荐技术栈

- UI 框架：Flutter
- 业务语言：Dart
- 状态管理：Riverpod 或 Bloc
- 本地存储：
  - MVP 可优先使用 SQLite
  - 若结构简单也可使用 Hive
- 桌面端能力扩展：Platform Channel
- 自动化测试：Dart 单元测试 + Widget 测试

### 6.6 Windows 端关键实现点

- 获取真实显示分辨率
- 保证全屏渲染
- 处理 DPI 感知
- 修正系统缩放对视标尺寸的影响
- 尽量以物理像素为最终基准进行绘制

### 6.7 手机端关键实现点

- 处理不同机型的分辨率与安全区域
- 提供横屏测试体验
- 处理刘海屏与异形屏布局
- 不依赖系统回报 DPI 作为唯一可信依据
- 仍以用户输入的物理宽高作为核心换算参数
- 高级版本可接入 ARKit / ARCore 用于距离提醒或动态视标修正

## 7. 测试逻辑与判定方案

### 7.1 基础规则

- 每次展示一个 E 视标
- 方向从上、下、左、右中随机选择
- 用户输入方向后立即判定正误
- 每一题都绑定一个明确的 logMAR 目标值
- 视标尺寸必须由当前 logMAR 与测试距离即时计算

### 7.2 档位组织原则

- 内部档位应按 logMAR 组织
- 推荐首版按 0.1 logMAR 递进
- 每个档位的视标尺寸按几何关系精确生成
- 不应只使用传统“第几行”作为唯一内部控制变量
- 对外主结果应换算为小数视力
- 对外辅助结果应换算为五分制对数视力

### 7.3 阈值判定方式

- 项目统一采用“标准化行级推进 + 逐视标记录”口径
- ETDRS 标准化流程下：
  - 每行固定 5 个视标
  - 每个视标只允许作答一次
  - 每行按逐视标正确数计分
  - 结果优先以 letter score 和等效 logMAR 计算
- 临床常见流程兼容下：
  - 允许按“最小通过行”记录结果
  - 行通过规则采用“该行正确数大于该行总数一半”
- 旧版阶梯法仅视为历史实验算法，不再作为项目规划默认实现

### 7.4 猜测概率处理

- 四选一任务存在 25% 机会水平
- 阈值设计必须避免把偶然猜对直接当作真实通过
- 系统至少应通过以下方式抑制猜测误差：
  - 最小题量
  - 连续档位验证
  - 重测一致性检查

### 7.5 显示约束与环境控制

- 每题渲染前应检查目标关键细节是否满足最小像素阈值
- 若设备像素能力不足，应提示用户增加测试距离或更换设备
- 测试前应提示合适环境照明
- 后续可扩展环境光检测与异常提示
- 拥挤效应模式应作为临床一致性增强选项纳入测试体系

### 7.6 终止条件

- 达到最小可测试档位且通过
- 当前行错误数超过协议允许阈值
- 4 米下正确数不足 ETDRS 近距补测门槛后进入近距补测
- 完成近距补测后得到标准化结果
- 达到工程保护题量上限 50
- 用户主动结束
- 设备像素条件不足时中止更高视力档位测试

### 7.7 结果输出建议

- 输出最终结果对应的小数视力
- 输出对应的五分制对数视力
- 输出内部计算用 logMAR 与 ETDRS letter score
- 输出测试时的距离
- 输出测试眼别
- 输出题量和正确率
- 输出可信度提示

## 8. 首版开发路线图

### 阶段 1：需求固化

- 确定首版只做远视力
- 确定首版只做 E 视标
- 确定首版测试距离标准
- 确定首版结果表达方式
- 确定主结果标准为小数视力
- 确定辅助结果标准为五分制对数视力
- 确定内部主计算体系为 logMAR
- 确定首发平台范围
- 确定采用 ETDRS 标准化流程作为测量引擎标准
- 确定临床常见行读流程只作为兼容映射
- 确定终止规则与题量保护规则

### 阶段 2：算法原型

- 建立物理宽高与分辨率换算模块
- 建立 logMAR 到视角的换算模块
- 建立视标尺寸映射公式
- 建立小数视力与五分制对数视力换算模块
- 建立测试档位表
- 建立行级推进规则与逐视标计分规则
- 建立像素瓶颈判定规则
- 对关键公式做样例校验

### 阶段 3：Windows MVP

- 初始化 Flutter 工程
- 完成设备参数录入页
- 完成测试准备页
- 完成视力学模型模块
- 完成 E 视标绘制
- 完成像素瓶颈判断
- 完成键盘输入
- 完成随机出题
- 完成档位控制
- 完成结果页
- 完成本地记录
- 处理 DPI 与缩放问题

### 阶段 4：移动端适配

- 适配 Android
- 适配 iOS
- 完成触控方向按钮输入
- 预留滑动手势输入
- 适配横屏流程
- 处理安全区域与异形屏

### 阶段 5：可靠性优化

- 增加每档位最小题量限制
- 增加答题反应时间记录
- 增加可信度提示
- 增加重测建议
- 增加历史趋势展示
- 增加跨设备一致性验证
- 增加银行卡/身份证校准
- 增加拥挤效应模式

### 阶段 6：体验打磨

- 优化测试前指引
- 优化输入反馈
- 优化错误恢复与重测流程
- 优化历史结果查看体验
- 优化跨平台显示一致性

## 9. 首版发布建议

### 9.1 平台优先级

- 第一优先：Windows
- 第二优先：Android
- 第三优先：iOS

### 9.2 首版范围建议

- 仅支持远视力
- 仅支持 E 视标
- 仅支持一个或少量固定测试距离
- 仅支持单设备本地记录
- 仅支持以小数视力为主结果、logMAR 为内部核心的标准化流程

### 9.3 成功标准

- 用户能够独立完成一次完整测试
- 视标尺寸在同设备上重复显示稳定
- 同一设备重复测试结果波动在可接受范围内
- Windows 缩放不会破坏核心尺寸换算
- 手机端与桌面端流程保持一致
- 小数视力与五分制对数视力之间换算一致且可追溯
- 屏幕像素瓶颈不会导致超范围误测

## 10. 项目结论

- 该应用的核心壁垒不是界面本身，而是“基于医学与物理原理的视敏度测量是否成立”
- 用户输入“屏幕物理宽度 + 屏幕物理高度”是合理且应坚持的方案
- 应以小数视力作为主结果标准，以五分制对数视力作为辅助显示，以 MAR / logMAR 作为内部主计算体系，并以 1 arcmin 为正常关键细节基准
- 分辨率读取、真实像素渲染、DPI 处理、自绘 E 视标，是首版工程重点
- 像素瓶颈、ETDRS 标准化计分、拥挤效应和环境约束，是提高测试可信度的重要补充
- 先做 Windows + Android 的专业化 MVP，是当前最稳妥的启动路径

## 11. 技术路径与技术栈定稿（V1）

### 11.1 技术路径定稿

- 路径原则：先“测量引擎正确”，再“跨端 UI 完整”，最后“可靠性增强”
- 实施顺序：
  - 第一步：固化纯 Dart 领域层与算法层
  - 第二步：接入 Flutter 页面与输入交互
  - 第三步：完成本地存储与历史回放
  - 第四步：做 Windows DPI 与移动端适配
  - 第五步：加入拥挤效应、可信度评分与重测建议
- 发布路径：
  - P0：Windows MVP（键盘输入、全屏、DPI 修正）
  - P1：Android 适配（触控输入、横屏流程）
  - P2：iOS 适配（安全区、异形屏一致性）

### 11.2 架构分层定稿

- `lib/vision/domain`：实体、枚举、配置模型、题目记录、结果结构
- `lib/vision/math`：视力学公式、尺寸换算、像素瓶颈、统计函数
- `lib/vision/testing`：题目推进、行级状态机、逐视标记录、标准化结果估计
- `lib/app`：页面状态流转、测试会话编排、异常与退出控制
- `lib/ui`：测试页、结果页、设备配置页、E 视标绘制与输入组件

### 11.3 技术栈定稿

- 编程语言：Dart 3
- 跨端框架：Flutter 3
- 状态管理：Riverpod
- 路由管理：GoRouter
- 本地数据库：SQLite（drift）
- 轻量键值存储：SharedPreferences
- 数据模型序列化：json_serializable
- 日志：logger
- 数学计算：Dart `dart:math`
- 自动化测试：flutter_test + Dart 单元测试

### 11.4 平台与工程约束

- Windows：
  - 必须保证 DPI 感知与缩放修正后尺寸仍按物理公式成立
  - 必须支持全屏测试与方向键低延迟输入
- Android / iOS：
  - 必须支持横屏测试主流程
  - 必须处理安全区域与异形屏边界
- 通用：
  - 视标渲染使用代码绘制，不使用位图缩放
  - 结果主显示为小数视力，辅助显示五分制对数视力
  - 任何档位提升前必须通过像素瓶颈检查

### 11.5 版本里程碑与交付物

- M1：核心引擎可用
  - 交付物：公式模块、渲染尺寸模块、标准化流程模块、单元测试样例
- M2：Windows MVP 可用
  - 交付物：设备配置页、测试页、结果页、本地历史、DPI 修正
- M3：移动端可用
  - 交付物：Android+iOS 触控流程、横屏适配、统一结果输出
- M4：可靠性增强
  - 交付物：拥挤效应模式、可信度指标、重测建议与趋势展示

## 12. 可执行任务清单（V1）

### 12.1 执行规则

- 每个任务均包含：目标、产出、验收标准
- 默认顺序执行，除标注“可并行”任务外不跳步
- 每完成一个里程碑，必须进行一次回归验证
- 任何影响尺寸计算与标准化结果判定的改动，必须补充样例测试

### 12.2 M1 核心引擎任务（纯 Dart）

#### 任务 1：领域模型定稿

- 目标：稳定 `domain` 层实体与枚举，作为后续模块的唯一数据契约
- 产出：
  - `ScreenProfile`、`CalibrationProfile`、`TestConfig`
  - `QuestionRecord`、`LineProgressState`、`EyeTestResult`
- 验收标准：
  - 字段覆盖项目规划中全部关键参数
  - 数据结构能完整表达一次单眼测试全流程

#### 任务 2：视力学公式模块

- 目标：完成 logMAR、MAR、小数视力、五分制对数视力互转
- 产出：
  - 公式函数集合（含正反向转换）
  - 角度换算与视标尺寸换算函数
- 验收标准：
  - 样例值与文档公式一致
  - 输入非法值时能给出明确异常

#### 任务 3：渲染尺寸与像素瓶颈模块

- 目标：完成物理尺寸到像素尺寸映射与安全阈值检查
- 产出：
  - `RenderMetrics` 计算逻辑
  - 像素瓶颈判定逻辑
- 验收标准：
  - 给定屏幕宽高和分辨率时，输出像素尺寸可复算
  - 关键细节像素低于阈值时能正确阻断档位继续提升

#### 任务 4：标准化测试流程模块

- 目标：落地 ETDRS 行级推进、逐视标记录、结果换算
- 产出：
  - 行级状态推进函数
  - ETDRS 计分函数
  - 临床常见行读映射函数
- 验收标准：
  - 行推进路径符合协议规则
  - 停止条件与结果换算正确

#### 任务 5：M1 单元测试最小集

- 目标：为核心计算与阈值逻辑建立可回归测试
- 产出：
  - 公式转换测试
  - 尺寸映射测试
  - 像素瓶颈测试
  - 标准化流程状态迁移测试
- 验收标准：
  - 关键样例全通过
  - 新增改动可通过测试快速发现回归

### 12.3 M2 Windows MVP 任务

#### 任务 6：项目骨架与基础页面

- 目标：建立 Flutter 应用壳与测试流程主页面
- 产出：
  - 设备配置页
  - 测试准备页
  - 测试执行页
  - 结果页
- 验收标准：
  - 可从首页完成一次完整流程跳转
  - 关键参数可在页面间正确传递

#### 任务 7：E 视标绘制组件

- 目标：实现参数化、可旋转的标准 E 视标绘制
- 产出：
  - 自绘 E 视标组件
  - 尺寸与方向控制接口
- 验收标准：
  - 5×5 结构成立
  - 四方向显示正确，尺寸与输入参数一致

#### 任务 8：键盘输入与题目控制

- 目标：实现方向键作答、判题与题目推进
- 产出：
  - 键盘事件映射
  - 随机方向出题器
  - 单题判定与状态更新
- 验收标准：
  - 输入延迟可接受
  - 每题记录可追溯到目标 logMAR 与用户答案

#### 任务 9：Windows DPI 与全屏适配

- 目标：保证 Windows 下尺寸换算与显示一致性
- 产出：
  - DPI 感知处理
  - 全屏渲染策略
- 验收标准：
  - 系统缩放变化不破坏核心尺寸逻辑
  - 全屏状态下测试区域稳定

#### 任务 10：本地存储与历史记录

- 目标：保存设备参数、测试会话、历史结果
- 产出：
  - 数据库表结构与访问层
  - 历史列表与详情查看
- 验收标准：
  - 关闭重开后数据可恢复
  - 历史记录字段完整可读

### 12.4 M3 移动端任务（Android / iOS）

#### 任务 11：移动端输入与横屏流程

- 目标：完成触控方向输入和横屏测试主流程
- 产出：
  - 方向按钮输入组件
  - 横屏页面布局
- 验收标准：
  - 单手可完成连续作答
  - 横屏下无遮挡、无错位

#### 任务 12：安全区与异形屏适配

- 目标：保证不同机型下布局安全和显示完整
- 产出：
  - 安全区处理方案
  - 刘海屏/打孔屏边界策略
- 验收标准：
  - 关键测试 UI 不被裁剪
  - 不同分辨率机型行为一致

### 12.5 M4 可靠性增强任务

#### 任务 13：拥挤效应模式

- 目标：提供 isolated 与 crowded 两种测试模式
- 产出：
  - 拥挤效应渲染方案
  - 模式切换与记录字段
- 验收标准：
  - 两种模式结果可区分、可追踪
  - 记录中明确标识测试模式

#### 任务 14：可信度指标与重测建议

- 目标：输出可解释的结果稳定性提示
- 产出：
  - 行级稳定性指标
  - 反应时统计指标
  - 重测建议规则
- 验收标准：
  - 结果页可显示可信度等级
  - 波动异常时能给出明确重测提示

#### 任务 15：趋势分析与回归验证

- 目标：支持历史趋势查看并固化回归验证流程
- 产出：
  - 历史趋势视图
  - 发布前回归检查清单
- 验收标准：
  - 可按眼别查看变化趋势
  - 发版前可按清单完成回归并记录结果

### 12.6 并行任务建议

- 可并行 A：任务 2（公式模块）与任务 6（页面骨架）
- 可并行 B：任务 7（绘制组件）与任务 10（存储模块）
- 可并行 C：任务 11（移动输入）与任务 12（安全区适配）

### 12.7 每周执行节奏建议

- 周目标固定为“2 个主任务 + 1 个验证任务”
- 周中完成开发，周末完成回归与文档更新
- 每周输出：
  - 完成任务列表
  - 未完成阻塞项
  - 下周任务承接计划

## 13. 15 个任务开发细化清单（可直接执行）

### 13.1 任务 1：领域模型定稿

- 开发动作：
  - 补齐实体字段约束（必填、可空、默认值）
  - 为核心实体增加 `copyWith` 与序列化能力
  - 统一枚举命名与取值边界
- 代码落点：
  - `lib/vision/domain/vision_enums.dart`
  - `lib/vision/domain/vision_models.dart`
- 接口清单：
  - `LineProgressState.copyWith(...)`
  - `ScreenProfile.pixelWidthMm / pixelHeightMm`
- 测试清单：
  - 模型构造合法性测试
  - 可空字段与默认值测试
  - `copyWith` 不可变性测试
- 完成定义：
  - 测试引擎运行所需字段无缺失
  - 所有核心实体可用于持久化与回放

### 13.2 任务 2：视力学公式模块

- 开发动作：
  - 固化 logMAR、MAR、小数视力、五分制互转
  - 补齐角度换算与尺寸换算边界检查
  - 增加非法输入保护
- 代码落点：
  - `lib/vision/math/vision_math.dart`
- 接口清单：
  - `marFromLogMar`
  - `logMarFromDecimal`
  - `fivePointFromLogMar`
  - `arcminToRad`
- 测试清单：
  - 1.0 / 0.5 / 0.1 视力样例测试
  - 互转回环误差测试
  - 非法输入抛错测试
- 完成定义：
  - 样例结果与文档公式一致
  - 回环误差在可接受浮点误差范围

### 13.3 任务 3：渲染尺寸与像素瓶颈模块

- 开发动作：
  - 实现物理尺寸到像素尺寸映射
  - 固化关键细节像素阈值判定
  - 输出统一 `RenderMetrics`
- 代码落点：
  - `lib/vision/math/vision_math.dart`
  - `lib/vision/domain/vision_models.dart`
- 接口清单：
  - `calculateRenderMetrics(...)`
  - `isPixelLimitReached(...)`
- 测试清单：
  - 已知屏幕参数复算测试
  - 阈值边界 3.0 / 2.0 / 1.9 测试
  - 横竖像素不一致场景测试
- 完成定义：
  - 每题渲染前可稳定给出 `criticalDetailPx`
  - 低于阈值时可阻断档位继续缩小

### 13.4 任务 4：标准化测试流程模块

- 开发动作：
  - 实现 ETDRS 行级推进
  - 实现逐视标原始记录
  - 实现 ETDRS letter score 与等效 logMAR 换算
  - 实现临床常见行读结果映射
- 代码落点：
  - `lib/vision/testing/`
- 接口清单：
  - `initialLineProgress(...)`
  - `submitOptotypeAnswer(...)`
  - `calculateEtDrsLetterScore(...)`
  - `calculateClinicalLineResult(...)`
- 测试清单：
  - 行内正确数统计测试
  - 错误数终止条件测试
  - ETDRS 分数换算测试
  - 行读映射测试
- 完成定义：
  - 状态迁移路径与标准一致
  - 结果可同时输出标准化结果与兼容结果

### 13.5 任务 5：M1 单元测试最小集

- 开发动作：
  - 建立 `test/vision/math`、`test/vision/testing` 用例
  - 覆盖公式、渲染、阈值三大核心逻辑
  - 增加回归用固定样例
- 代码落点：
  - `test/vision/math/vision_math_test.dart`
  - `test/vision/testing/`
- 测试清单：
  - 文档样例等值测试
  - 边界值测试
  - 回归样例快照测试
- 完成定义：
  - 核心测试可一键执行
  - 关键逻辑回归可自动发现

### 13.6 任务 6：项目骨架与基础页面

- 开发动作：
  - 初始化 `lib/app` 路由与状态壳
  - 建立配置页、准备页、测试页、结果页
  - 接通页面参数传递链路
- 代码落点：
  - `lib/app/router.dart`
  - `lib/ui/pages/device_config_page.dart`
  - `lib/ui/pages/test_prepare_page.dart`
  - `lib/ui/pages/test_run_page.dart`
  - `lib/ui/pages/test_result_page.dart`
- 测试清单：
  - 路由跳转测试
  - 参数透传测试
  - 页面生命周期稳定性测试
- 完成定义：
  - 可从首页跑通一条完整测试流程
  - 页面间状态不丢失

### 13.7 任务 7：E 视标绘制组件

- 开发动作：
  - 实现 `CustomPainter` 自绘 E 视标
  - 参数化控制尺寸、方向、颜色
  - 增加拥挤效应绘制预留参数
- 代码落点：
  - `lib/ui/widgets/e_optotype_painter.dart`
  - `lib/ui/widgets/e_optotype_view.dart`
- 测试清单：
  - 5×5 结构视觉校验
  - 四方向旋转一致性测试
  - 多尺寸渲染清晰度检查
- 完成定义：
  - 不依赖位图即可清晰渲染目标 E
  - 尺寸与 `RenderMetrics` 对齐

### 13.8 任务 8：键盘输入与题目控制

- 开发动作：
  - 建立方向键映射与事件防抖
  - 集成随机方向题目生成
  - 打通判题后状态更新与下一题推进
- 代码落点：
  - `lib/app/test_session_controller.dart`
  - `lib/ui/pages/test_run_page.dart`
- 接口清单：
  - `submitAnswer(OptotypeDirection direction)`
  - `nextQuestion()`
- 测试清单：
  - 键盘输入响应测试
  - 随机方向分布基础测试
  - 题目记录完整性测试
- 完成定义：
  - 单题判定、记录、推进可连续运行
  - 方向键输入无明显漏判

### 13.9 任务 9：Windows DPI 与全屏适配

- 开发动作：
  - 接入 Windows DPI 感知策略
  - 实现全屏切换与状态保持
  - 建立缩放变化后的尺寸复算流程
- 代码落点：
  - `windows/runner/*`
  - `lib/app/platform/windows_display_service.dart`
- 测试清单：
  - 100% / 125% / 150% 缩放测试
  - 多分辨率显示器测试
  - 全屏切换稳定性测试
- 完成定义：
  - 缩放变化不导致目标视标尺寸偏离公式值
  - 全屏流程可稳定执行完整测试

### 13.10 任务 10：本地存储与历史记录

- 开发动作：
  - 设计 SQLite 表结构与索引
  - 建立仓储层与查询接口
  - 实现历史列表与详情展示
- 代码落点：
  - `lib/data/local/database.dart`
  - `lib/data/local/dao/*.dart`
  - `lib/ui/pages/history_page.dart`
- 测试清单：
  - 插入/查询/更新一致性测试
  - 重启后数据恢复测试
  - 历史筛选测试（眼别、日期）
- 完成定义：
  - 测试会话可完整持久化
  - 历史记录可追溯关键参数

### 13.11 任务 11：移动端输入与横屏流程

- 开发动作：
  - 实现触控方向按钮与手势预留
  - 适配横屏主流程布局
  - 统一与桌面端的判题逻辑入口
- 代码落点：
  - `lib/ui/widgets/direction_pad.dart`
  - `lib/ui/pages/test_run_page.dart`
- 测试清单：
  - 连续点击输入测试
  - 横屏布局稳定性测试
  - 不同屏幕尺寸适配测试
- 完成定义：
  - 移动端可完成完整单眼测试
  - 输入链路与桌面端结果一致

### 13.12 任务 12：安全区与异形屏适配

- 开发动作：
  - 统一安全区容器策略
  - 处理刘海/打孔区域避让
  - 修复高宽比极端机型布局
- 代码落点：
  - `lib/ui/layout/adaptive_scaffold.dart`
  - `lib/ui/pages/*`
- 测试清单：
  - 常见异形屏机型预览测试
  - 字体缩放场景测试
  - 横竖切换回归测试
- 完成定义：
  - 关键信息不被遮挡
  - 测试页元素无重叠和越界

### 13.13 任务 13：拥挤效应模式

- 开发动作：
  - 增加 `isolated/crowded` 模式开关
  - 实现 flankers 或边框干扰绘制
  - 将模式写入题目与结果记录
- 代码落点：
  - `lib/vision/domain/vision_enums.dart`
  - `lib/ui/widgets/e_optotype_view.dart`
  - `lib/app/test_session_controller.dart`
- 测试清单：
  - 模式切换渲染测试
  - 记录字段正确性测试
  - 结果页模式标识测试
- 完成定义：
  - 两种模式可独立运行并区分输出
  - 历史记录可按模式筛选

### 13.14 任务 14：可信度指标与重测建议

- 开发动作：
  - 计算行级稳定性、正确率、平均反应时
  - 制定可信度等级与重测规则
  - 在结果页展示解释文本
- 代码落点：
  - `lib/vision/testing/`
  - `lib/app/result_interpreter.dart`
  - `lib/ui/pages/test_result_page.dart`
- 测试清单：
  - 高稳定/低稳定对比测试
  - 超时过多场景测试
  - 重测建议触发测试
- 完成定义：
  - 结果解释具备一致规则
  - 可对不稳定结果给出明确行动建议

### 13.15 任务 15：趋势分析与回归验证

- 开发动作：
  - 建立历史趋势数据聚合
  - 实现按眼别趋势图与摘要卡片
  - 固化发布前回归清单
- 代码落点：
  - `lib/data/analytics/history_analytics_service.dart`
  - `lib/ui/pages/history_trend_page.dart`
  - `项目规划.md` 回归清单段落
- 测试清单：
  - 多次测试趋势生成测试
  - 异常波动提醒测试
  - 发布前回归清单执行验证
- 完成定义：
  - 可视化展示趋势并支持筛选
  - 每次发布前有统一回归基线

### 13.16 执行顺序与首期落地建议

- 首期优先顺序：1 → 2 → 3 → 4 → 5 → 6 → 7 → 8 → 10 → 9
- 第二期顺序：11 → 12 → 13 → 14 → 15
- 首期完成门槛：
  - M1 全通过
  - Windows MVP 可完成单眼全流程
  - 历史记录可保存与回看

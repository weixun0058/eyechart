enum EyeSide {
  left,
  right,
  both,
}

enum OptotypeDirection {
  up,
  down,
  left,
  right,
}

enum TestMode {
  isolated,
  crowded,
}

enum InputMode {
  keyboard,
  touchButtons,
  swipe,
}

enum SessionEndReason {
  thresholdReached,
  maxQuestionsReached,
  pixelLimitReached,
  userAborted,
}

enum StepDirection {
  up,
  down,
}

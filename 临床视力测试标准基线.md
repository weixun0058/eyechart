# 临床视力测试标准基线

## 1. 文档目的

- 本文档用于把项目后续涉及的视力测试流程标准统一到一处，避免“工程试验口径”和“临床流程口径”混用。
- 本文档只定义远视力测试相关的流程基线，不覆盖验光、屈光矫正、近视力、散光、色觉等其他检查。
- 本文档从现在开始作为本项目的唯一上位标准来源。

## 2. 标准层级

- 第一层：ETDRS 标准化流程
  - 用于项目后续测量引擎、结果计算、术语定义和数据结构设计。
  - 当“常见临床做法”和“ETDRS 标准化流程”冲突时，以 ETDRS 口径为准。
- 第二层：临床常见视力表流程
  - 用于描述国内常见 5 米 E 视力表/标准对数视力表的日常检查流程。
  - 该流程可作为兼容模式、简化筛查模式或结果展示映射依据。
- 废止口径：
  - `3-down 1-up`
  - `6 次反转终止`
  - `反转点均值估计阈值`
  - 上述内容仅属于此前工程试验算法，不再作为项目标准。

## 3. 共同不变原则

- 测试对象：远视力。
- 视标几何：标准 E 视标采用 `5 x 5` 单元结构。
- 关键细节：1 个笔画宽度或 1 个缺口宽度对应 1 个 MAR。
- 结果表达：
  - 主显示：小数视力。
  - 辅助显示：五分制对数视力。
  - 内部计算：logMAR / MAR。
- 测试必须记录测试距离。
- 非受检眼遮盖应使用不透光遮挡，且不得压迫眼球。
- 视标尺寸必须由测试距离和视觉角度反推，不得使用经验字号替代。

## 4. 临床常见视力表流程

### 4.1 适用对象

- 适用于国内门诊、体检、筛查中常见的 5 米标准对数视力表或 E 视力表流程。
- 该流程强调“逐行读取 + 行通过判定”，不是 ETDRS 的逐字母计分流程。

### 4.2 常见检查条件

- 标准距离通常为 `5 m`。
- 视力表应置于光线合适、对比度足够、尽量无反光干扰的环境。
- `0` 线或 `1.0` 所在水平应尽量与受检眼平齐。
- 常规顺序通常为：
  - 右眼。
  - 左眼。
  - 必要时双眼。

### 4.3 常见操作流程

1. 说明测试规则，要求受检者指出 E 的开口方向。
2. 遮盖非受检眼，不能压迫眼球。
3. 受检眼在 `5 m` 处，从大视标向小视标逐行识别。
4. 对每一行按该行视标总数进行通过判断。
5. 记录“能够通过的最小一行”作为该眼视力结果。

### 4.4 常见记录规则

- 常见行读式规则：
  - 受检者在某一行正确识别数 `> 该行视标总数的一半`，该行判为通过。
  - 该眼视力记录为“通过的最小一行”。
- 常见记录方式：
  - 小数视力。
  - 五分制对数视力。
- 若在 `5 m` 不能识别最大一行，则按更近距离复测后折算记录。

### 4.5 该流程的局限

- 行内视标数量、行间间距、拥挤效应控制、照明一致性在不同场景下不完全统一。
- 该流程更适合门诊常规筛查，不等同于 ETDRS 的标准化研究级流程。
- 该流程更容易受行布局、熟悉度和记录习惯影响。

## 5. ETDRS 标准化流程

### 5.1 流程定位

- ETDRS 是国际临床研究中广泛使用的标准化远视力测量流程。
- 本项目后续涉及“测量引擎、结果判定、数据结构”的统一标准，以该流程为最高优先级。

### 5.2 图表与结构特征

- 标准测试距离通常为 `4 m`。
- 每行固定 `5` 个视标。
- 相邻行大小按固定对数步长递进，每行相差 `0.1 logMAR`。
- 每个字母或视标相当于 `0.02 logMAR`。
- 行内间距、行间间距按标准化几何比例设置，以尽量保持稳定拥挤效应。
- 标准化照明条件通常依赖灯箱或等效受控亮度环境。

### 5.3 标准化操作流程

1. 说明规则，要求受检者逐个识别当前行中的每一个视标。
2. 遮盖非受检眼，不能压迫眼球。
3. 受检眼在 `4 m` 处，从大到小逐行读取。
4. 每个视标只允许作答一次。
5. 当某一行错误数超过允许阈值时停止继续向更小行推进。
6. 结果以逐视标计分，而不是只记录“最后通过的一行”。

### 5.4 常见 ETDRS 记录规则

- 常见逐行停止口径：
  - 从上往下读。
  - 当某一行错误数 `> 2` 时停止。
- 常见逐字母计分口径：
  - 在 `4 m` 正确识别 `>= 20` 个字母时，`letterScore = correctAt4m + 30`。
  - 若在 `4 m` 正确识别 `< 20` 个字母，则转到 `1 m` 继续读取前 6 行可见字母，按标准补充分数。
- 等效 logMAR 可由逐字母分数换算：
  - `logMAR = 1.7 - 0.02 x letterScore`
- 若只采用行级输出，也必须保留逐字母原始记录，以避免信息丢失。

### 5.5 ETDRS 与常见临床行读法的本质差异

- ETDRS 核心是“标准化布局 + 逐字母计分 + 对数等步长”。
- 常见临床流程核心是“逐行读取 + 行通过判定 + 最小通过行记录”。
- ETDRS 结果更适合统计分析、纵向比较和跨场景一致性控制。
- 常见临床流程更贴近日常门诊与筛查操作，但标准化程度较低。

## 6. 本项目的统一约定

### 6.1 项目标准选择

- 测量标准：采用 ETDRS 标准化流程。
- 显示与用户沟通：输出小数视力和五分制对数视力。
- 临床兼容：保留“常见临床行读式结果”的映射与对照能力。

### 6.2 对现有工程方案的约束

- 不再把“反转次数”作为视力测试标准术语。
- 不再把“连续答对 3 次降档、答错 1 次升档”作为项目默认流程。
- 不再把“最后若干次反转点均值”作为最终视力结果的标准计算方式。
- 后续代码修改必须围绕：
  - 行级/字母级呈现顺序。
  - 行级停止条件。
  - 逐字母或逐视标原始记录。
  - ETDRS 分数与等效 logMAR。
  - 临床常见行读结果映射。

### 6.3 对数据结构的约束

- 会话配置必须显式保存 `protocol`。
- 题目记录必须能表达：
  - 当前行。
  - 当前视标在该行中的序号。
  - 目标 logMAR。
  - 当前答题是否正确。
- 结果结构必须同时支持：
  - `bestLineLogMar`
  - `bestDecimalAcuity`
  - `bestFivePointAcuity`
  - `etdrsLetterScore`
  - `usedNearFallback`
- 若保留旧版 `staircase` 相关字段，必须明确标记为“迁移期兼容字段”，不得再作为权威结果来源。

### 6.4 对产品交互的约束

- 测试中对用户显示：
  - 当前正在测的“小数视力等级”。
  - 当前正在测的“五分制对数视力等级”。
- 测试主界面默认不把 `logMAR` 作为面向普通用户的主展示项。
- 题量上限作为工程保护项可设为 `50`，但它不是医学标准本身。

## 7. 后续文档同步规则

- `项目规划.md` 必须与本文件一致。
- `视力学公式与数据结构设计稿.md` 必须与本文件一致。
- 需求规格文档若涉及测试流程，必须引用本文件，不得自定义另一套默认规则。
- README、临时分析文档、旧讨论稿如仍出现“阶梯法主流程”表述，应视为待清理历史内容。

## 8. 参考来源

1. ETDRS 与标准对数视力表比较研究（PMC）  
   https://pmc.ncbi.nlm.nih.gov/articles/PMC8025170/
2. NCBI 资料中对 ETDRS 字母计分方式的说明  
   https://www.ncbi.nlm.nih.gov/books/NBK539248/
3. StatPearls: Evaluation of Visual Acuity  
   https://www.ncbi.nlm.nih.gov/books/NBK564307/
4. DICOM 说明中关于 ETDRS 为事实标准、传统视力表精度较低的说明  
   https://dicom.nema.org/dicom/2013/output/chtml/part17/sect_RR.2.html

## 9. 当前状态说明

- 本文档已经把“临床常见流程”和“ETDRS 标准化流程”分开记录。
- 本文档已经明确项目采用哪一套作为上位标准。
- 代码尚未完成同步替换，当前仓库内若仍存在 `staircase`、`reversal` 相关实现，属于历史实现状态，不代表当前标准已经落地。

# 当前视力测试逻辑分析

> 生成时间：2026-04-14
> 基于代码实际实现，独立于基线文档

---

## 一、测试流程逻辑

### 1.1 测试配置参数（TestConfig）

| 参数 | 默认值 | 说明 |
|------|--------|------|
| `startLogMar` | 0.5 | 测试起始 logMAR 值 |
| `minLogMar` | -0.3 | 最小 logMAR（最好视力） |
| `maxLogMar` | 1.0 | 最大 logMAR（最差视力） |
| `stepLogMar` | 0.1 | 行间步长 |
| `optotypesPerLine` | 5 | 每行视标数量 |
| `maxErrorsPerLine` | 1 | 每行最大允许错误数（错2个停止） |
| `minCorrectPerLine` | 3 | 每行通过所需最少正确数 |
| `requiredLineReversals` | 2 | 需要完成的行级反转次数 |
| `maxQuestionCount` | 30 | 最大题目数量（工程保护） |
| `answerTimeLimitMs` | 3000 | 每题答题时限 |

### 1.2 测试状态（LineProgressState）

```
currentLineIndex: 当前行索引（从0开始）
currentLogMar: 当前行的 logMAR 值
currentLinePresentedCount: 当前行已呈现视标数
currentLineCorrectCount: 当前行正确数
currentLineErrorCount: 当前行错误数
totalPresentedCount: 总呈现视标数
totalCorrectCount: 总正确数
lineDirection: 当前方向（down=向下/更好视力，up=向上/更差视力）
lineReversalCount: 已发生的反转次数
protocolCompleted: 协议是否完成
```

### 1.3 答题处理流程（applyAnswer）

**每次答题后更新统计：**
1. 当前行呈现数 +1
2. 当前行正确/错误数 +1
3. 总呈现数 +1
4. 总正确数 +1（如果答对）

**判断逻辑：**

```
if (当前行错误数 > maxErrorsPerLine) {
    // 行失败，反转方向上行
    执行反转逻辑
} else if (当前行呈现数 >= optotypesPerLine) {
    // 当前行完成
    if (当前行正确数 >= minCorrectPerLine) {
        // 行通过，继续下行
        移动到下一行
    } else {
        // 行未通过，反转方向上行
        执行反转逻辑
    }
} else {
    // 当前行未完成，继续当前行
    保持当前行状态
}
```

### 1.4 反转逻辑（_reverseDirection）

**触发条件：**
- 当前行错误数超过阈值（>1）
- 当前行完成但未通过（正确数<3）

**反转流程：**
1. 反转次数 +1
2. 方向切换（down↔up）
3. 检查是否达到 requiredLineReversals（2次）
   - 达到：标记 protocolCompleted = true，测试结束
   - 未达到：移动到相邻行

**移动规则：**
- 原方向 down → 新方向 up：移动到上一行（lineIndex - 1）
- 原方向 up → 新方向 down：移动到下一行（lineIndex + 1）

**边界检查：**
- 如果超出 minLogMar/maxLogMar 范围，标记测试完成

### 1.5 测试终止条件（TestSessionController._submitAnswerInternal）

终止优先级（从高到低）：

1. **协议完成** (`protocolCompleted = true`)
   - 原因：`SessionEndReason.protocolCompleted`

2. **达到最大题数** (`questions.length >= maxQuestionCount`)
   - 原因：`SessionEndReason.maxQuestionsReached`
   - 当前设置：30题

3. **像素限制** (`pixelLimitReached = true`)
   - 原因：`SessionEndReason.pixelLimitReached`

4. **用户主动退出**
   - 原因：`SessionEndReason.userAborted`

---

## 二、视力结果判定逻辑

### 2.1 结果构建流程（buildEyeTestResult）

**输入：**
- 所有答题记录（List<QuestionRecord>）
- 测试配置（TestConfig）
- 是否遇到像素限制

**输出：** EyeTestResult 对象

### 2.2 基础统计计算

```dart
totalQuestions = 答题总数
correctQuestions = 正确答题数
accuracy = correctQuestions / totalQuestions
meanResponseTimeMs = 所有答题反应时间的平均值
```

### 2.3 ETDRS 风格结果计算

**字母分数（letterScore）：**
```
letterScore = totalCorrect + 30
```

**等效 logMAR：**
```
equivalentLogMar = 1.7 - 0.02 × letterScore
```

**示例计算：**
| 答对题数 | letterScore | equivalentLogMar | 小数视力 | 五分视力 |
|---------|-------------|------------------|----------|----------|
| 0 | 30 | 1.1 | 0.08 | 3.9 |
| 10 | 40 | 0.9 | 0.13 | 4.1 |
| 20 | 50 | 0.7 | 0.20 | 4.3 |
| 30 | 60 | 0.5 | 0.32 | 4.5 |
| 40 | 70 | 0.3 | 0.50 | 4.7 |
| 50 | 80 | 0.1 | 0.80 | 4.9 |

**小数视力转换：**
```
decimalAcuity = 10^(-equivalentLogMar)
```

**五分制视力转换：**
```
fivePointAcuity = 5 - equivalentLogMar
```

### 2.4 最佳行计算（calculateBestLineLogMar）

**流程：**
1. 按行索引分组统计每行的正确/错误数
2. 遍历所有行，找到满足条件的行：
   - 正确数 >= minCorrectPerLine（3个）
3. 在通过的行中，找到行索引最大的（视力最好的一行）
4. 返回该行的 logMAR 值

**如果没有行通过：**
- 返回 startLogMar（默认 0.5）

### 2.5 行级反转点收集（collectLineReversals）

**流程：**
1. 按行索引排序所有答题记录
2. 遍历每一行，推断该行的方向：
   - 第一行：down
   - 行索引增加：down
   - 行索引减少：up
3. 检测方向变化，记录为反转点
4. 每个反转点记录：
   - 行索引
   - logMAR 值
   - 原方向
   - 新方向
   - 该行正确数
   - 该行错误数

### 2.6 重测建议判定（_shouldRecommendRetest）

**条件：**
```dart
if (反转点数量 < 2) return false;

// 计算反转点之间的 logMAR 范围
range = max(logMarValues) - min(logMarValues);

return range > 0.3;  // 差异超过0.3建议重测
```

---

## 三、关键问题识别

### 3.1 30题上限问题

**现状：**
- maxQuestionCount = 30
- 终止优先级：协议完成 > 最大题数 > 像素限制

**潜在问题：**
- 对于视力较好的用户，可能需要超过30题才能完成2次反转
- 30题可能在反转完成前强制终止测试

### 3.2 行级反转检测问题

**现状：**
- 反转点通过事后分析答题记录推断
- 而不是在状态转换时实时记录

**潜在问题：**
- 反转检测可能不准确
- 重测建议可能不可靠

### 3.3 结果计算基于不完整数据

**现状：**
- 如果因30题上限终止，protocolCompleted = false
- 但结果仍然被计算并显示

**潜在问题：**
- ETDRS letterScore 基于不完整的测试数据
- 结果可信度降低

---

## 四、待修改点清单

1. [ ] 调整 maxQuestionCount（30 → ?）
2. [ ] 调整终止条件优先级
3. [ ] 修复行级反转记录逻辑
4. [ ] 处理不完整测试的结果显示
5. [ ] 清理废弃的阶梯法参数
6. [ ] 其他（待补充）

---

## 五、新版测试逻辑确认（2026-04-14 对话确认）

### 5.1 对话确认要点

基于用户反馈，新版测试逻辑的关键确认点：

| 确认问题 | 用户答复 |
|---------|---------|
| 连续答对3题是否必须是前3题？ | **必须是该行的前3题** |
| 连续答错2题是否必须是前2题？ | **不一定是前2题，任意位置连续2错即判定失败** |
| 前3题只有1错时，5题后判定标准？ | **5题后错误数≤1则通过** |
| 反转计数规则？ | **只计下行→上行的变化** |
| 最终结果？ | **取最终确定的视力值，ETDRS letterScore仅作参考** |

### 5.2 新版行内判断规则

| 场景 | 判定条件 | 操作 |
|------|---------|------|
| **快速通过** | 前3题全对 | 行通过，视力暂定为当前行，跳到下一行（下行） |
| **快速失败** | 任意位置连续2错 | 行失败，视力暂定为向上一级（更大视标），跳到上一行（上行） |
| **完整测试** | 前3题只有1错 | 必须答完5题，再根据结果判断：错误数≤1则通过，否则失败 |

### 5.3 新版反转计数规则

- **只统计下行→上行的方向变化**
- 上行→下行不算反转
- 达到 `requiredLineReversals`（默认3次）下行→上行反转时终止测试

### 5.4 示例流程

```
起始：第1行（0.5 logMAR）

第1行：
- 题1：对
- 题2：对  
- 题3：对 → 前3题全对，行通过，视力暂定0.5，下行到第2行（0.4）

第2行：
- 题1：错
- 题2：对
- 题3：错 → 不连续，继续
- 题4：错 → 题3+题4连续2错，行失败，视力暂定0.5（回退），上行到第1行
- 第1次反转（下行→上行）✓

第1行（第2次经过）：
- 题1：对
- 题2：对
- 题3：错 → 前3题1错，必须答完5题
- 题4：对
- 题5：对 → 5题共1错，行通过，视力暂定0.5，下行到第2行
- （上行→下行，不算反转）

...直到第3次下行→上行反转后终止
```

### 5.5 新版参数配置结构

```dart
class TestConfig {
  // === 视力范围参数 ===
  final double startLogMar;      // 起始视力：0.5
  final double minLogMar;        // 最好视力：-0.3
  final double maxLogMar;        // 最差视力：1.0
  final double stepLogMar;       // 行间步长：0.1
  
  // === 行内判断参数 ===
  final int optotypesPerLine;           // 每行视标数：5
  final int consecutiveCorrectToPass;   // 前几题连续对直接通过：3
  final int consecutiveWrongToFail;     // 连续错几题直接失败：2
  final int maxErrorsPerLine;           // 整行最大允许错误数：1（即至少对4题才算通过）
  
  // === 终止条件参数 ===
  final int requiredLineReversals;      // 需要几次反转终止：3
  
  // === 工程保护参数 ===
  final int maxQuestionCount;           // 答题上限：999（不设限）
  final int answerTimeLimitMs;          // 答题时限：3000ms
}
```

### 5.6 新版测试结果判定

**主结果**：
- 最终确定的视力值（小数视力 + 五分制视力）
- 取自最后一次成功通过的行（或失败后回退的行）

**参考值**：
- ETDRS letterScore = totalCorrect + 30
- 仅用于统计分析参考，不作为主要结果

---

## 六、待修改点清单（新版）

| 序号 | 修改内容 | 状态 | 说明 |
|------|---------|------|------|
| 1 | 移除 maxQuestionCount 限制（设为999或更大） | ✅ 已完成 | `test_config_provider.dart` 中 `maxQuestionCount: 999` |
| 2 | 修改行内判断逻辑（前3题全对/任意连续2错/前3题1错则答完5题） | ✅ 已完成 | `adaptive_line_engine.dart` 中已实现快速通过/失败逻辑 |
| 3 | 修改反转计数逻辑（只计下行→上行） | ✅ 已完成 | `_reverseToPreviousLine` 方法中只统计下行→上行反转 |
| 4 | 修改终止条件（3次下行→上行反转） | ✅ 已完成 | `requiredLineReversals: 3`，达到后 `protocolCompleted = true` |
| 5 | 更新 TestConfig 参数结构 | ✅ 已完成 | 新增 `consecutiveCorrectToPass`、`consecutiveWrongToFail`、`requiredLineReversals` 参数 |
| 6 | 更新结果计算（主结果取最终确定视力，letterScore仅作参考） | ✅ 已完成 | `determinedLogMar` 字段记录最终视力，letterScore 仅作参考 |
| 7 | 清理废弃的阶梯法参数 | ✅ 已完成 | `staircase_estimator.dart` 已标记 `@Deprecated`，硬编码默认值兼容旧代码 |

**验证状态：**
- ✅ `adaptive_line_engine_test.dart` 全部12个测试通过
- ✅ `flutter analyze` 无错误
- ✅ 代码已打包并安装测试

---

## 七、实现文件清单

| 文件路径 | 作用 |
|---------|------|
| `lib/vision/testing/adaptive_line_engine.dart` | 新版自适应行级测试引擎核心逻辑 |
| `lib/vision/domain/vision_models.dart` | TestConfig、LineProgressState 数据模型 |
| `lib/app/providers/test_config_provider.dart` | 测试配置 Provider，集中控制所有参数 |
| `lib/app/test_session_controller.dart` | 测试会话控制器，使用 AdaptiveLineEngine |
| `test/vision/testing/adaptive_line_engine_test.dart` | 单元测试，覆盖所有测试场景 |

---

*此文档基于代码实际实现，用于逐条讨论修改方案*
*新版逻辑已根据用户对话确认（2026-04-14）*
*待修改清单更新于：2026-04-15*
